# SRMS---Student-Record-Management-System-
A simple and modular Student Management System in C that uses text files for persistent storage. Includes login authentication, role-based access control, and CRUD operations (Add, Display, Search, Update, Delete). Supports different permissions for Admin, Staff, and Guest users. 
